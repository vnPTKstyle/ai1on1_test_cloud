# Server
PORT=8080

# MySQL 8.0+
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=test_cloud_run

# JWT (generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Optional: AWS (for S3, Comprehend)
# AWS_REGION=ap-northeast-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# S3_BUCKET=

# Optional: OpenAI
# OPENAI_API_KEY=
